<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.thinkjoy.gaokao360.dao.ex.IMajoredExDAO">
<!--
insert
update
updateNull
deleteById
deleteByCondition
deleteByProperty
fetch
findOne
findList
findAll
queryPage
like
queryList
queryOne
count
selectMaxId
updateOrSave
selectOne
selectList
-->

    <sql id="Base_Column_List">
        id as id ,
        code as code ,
        name as name ,
        educationLevel as educationLevel ,
        subjectType as subjectType ,
        majoredType as majoredType ,
        degree as degree ,
        similarMajored as similarMajored ,
        mainStudy as mainStudy ,
        mainCourse as mainCourse ,
        workGuide as workGuide ,
        university as university ,
        stressUniversity as stressUniversity ,
        other as other ,
        jobRat as jobRat ,
        incomeAve as incomeAve ,
        jobMajoredRel as jobMajoredRel ,
        salaryRank as salaryRank ,
        employedRank as employedRank ,
        subjectTypeId as subjectTypeId ,
        majoredTypeId as majoredTypeId
    </sql>

    <select id="queryPage" resultType="Majored">
        SELECT
        <include refid="Base_Column_List" />
        FROM gk_majored
        <where>

            1=1
            <if test="condition.whereSql != null">
                and id in (#{condition.whereSql})
            </if>
            <if test="condition.subjectType!=null">
                ${condition.groupOp}   ${condition.subjectType.field}  ${condition.subjectType.op}  #{condition.subjectType.data}
            </if>
            <if test="condition.queryparam!=null">
                ${condition.groupOp}
                (code like #{condition.queryparam.data}
                OR name like #{condition.queryparam.data}
                OR name like #{condition.queryparam.data}
                OR salaryRank like #{condition.queryparam.data}
                OR employedRank like #{condition.queryparam.data}
                OR majoredType like #{condition.queryparam.data})
            </if>


        </where>
            ORDER BY id DESC
        <if test="offset != null">
            limit ${offset}, ${rows}
        </if>
    </select>

    <select id="count" resultType="java.lang.Integer">
        SELECT count(id) FROM gk_majored
        <where>
1=1
            <if test="subjectType!=null">
                ${groupOp}   ${subjectType.field}  ${subjectType.op}  #{subjectType.data}
            </if>
            <if test="queryparam!=null">
                ${groupOp}
                (code like #{queryparam.data}
                OR name like #{queryparam.data}
                OR name like #{queryparam.data}
                OR salaryRank like #{queryparam.data}
                OR employedRank like #{queryparam.data}
                OR majoredType like #{queryparam.data}
                OR subjectType like #{queryparam.data})
            </if>

        </where>
    </select>
    <select id="getMajoredListByName" resultType="cn.thinkjoy.gaokao360.domain.GkBaseDomain">
        SELECT id,name,subjectType as type FROM gk_majored
         <where>
             <if test="name!=null">
                 AND (name like CONCAT('%', #{name} , '%'),name subjectType CONCAT('%', #{name} , '%'))
             </if>
         </where>
    </select>


</mapper>

